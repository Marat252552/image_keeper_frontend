export type LoginValues_T = {
    login: string,
    password: string,
    remember: boolean
}

export type SigninValues_T = {
    login: string,
    password: string,
    password2: string,
    remember: boolean
}